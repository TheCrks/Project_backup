#from urllib.parse import urlparse
#from reppy.robots import Robots

def urlAllowed(url):
    #parsedUrl = urlparse(url)
    #robotsUrl = parsedUrl.scheme+"://"+parsedUrl.netloc+"/robots.txt"
    
    #robots = Robots.fetch(robotsUrl)
    #isAllowed = robots.allowed(url,'*')
    #return isAllowed
    return True
